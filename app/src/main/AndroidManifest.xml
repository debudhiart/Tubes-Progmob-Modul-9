<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.aryad.sipasar">

    <application
        android:name="id.aryad.sipasar.RincianPengeluaranData"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ModulPencatatanPengeluaranPasarLainnya">
        <activity android:name=".ViewRincianPengeluaranActivity"></activity>
        <activity android:name=".EditRincianPengeluaranActivity" />
        <activity android:name=".TambahRincianPengeluaranActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".LoginManagerActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>